[pytest]
testpaths = tests
addopts = -ra -m "not hardware"
markers =
    integration: requires an external recording or environment
    hardware: requires explicit Maxwell hardware access; never part of replay verification
timeout = 30
